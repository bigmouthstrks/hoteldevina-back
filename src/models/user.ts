export interface UserData {
    firstName: string;
    lastName: string;
    userId?: number;
    email: string;
    password: string;
    role: string;
    documentType?: string;
    documentNumber?: string;
}
